extends layout

block content
    style.
        .treeSelectPanel ul li{
            display: inline-block;
            margin-right: 10px;
        }
    div(style="width: 1000px; margin: 0 auto;")
        h2 Demo - 树形选择框 TreeSelect
        
        div.treeContainer
        //- div.uploaderContainer UploaderTitle
        p.alert.alert-info.mt30 此控件依赖于jQuery，在使用前请先在页面引入jQuery。<br>CSV文件的编码格式须为UTF-8.
        p 使用步骤说明：
        ol.pb50
            li 引入js文件，
                a(href="/dest/oc.js", target="_blank") oc.js
                span , 或者
                a(href="/dest/oc.min.js", target="_blank") 压缩版 oc.min.js
            li 引入css文件：通过传统在head中添加link标签 或者在script中添加指向css路径的“data-occss”属性 script src="/dest/oc.min.js" data-occss="/dest/oc.css"
                | a(href="/dest/oc.css", target="_blank") oc.css
            li new oc.FileView('.fileViewContainer');  fileViewContainer一般为一个div容器。
            li 本页面的伪代码如下
                pre
                    |doctype html
                    |html(lang="zh-CN")
                    | head
                    |   title Test
                    |   script(src="/js/jquery-2.1.3.min.js")
                    |   script(src="/dest/oc.min.js", data-occss="/dest/oc.css")
                    |  body
                    |   div.fileViewContainer
                    | 
                    |   script.
                    |       var fileView = new oc.FileView('.fileViewContainer');
                    |       console.log(fileView.getDataList());


        span 友情链接： 
            a(href="/tree") 树形组件

                
    script(src="/mockData/tree.json")
    script.
        var dataList = TREEDATA;
        var treeSelect = new oc.TreeSelect({
            dataList: dataList.items, 
            container: '.treeContainer', 
            iptClass: 'form-control input-sm', 
            width: '200px'
        });
      
        console.log(treeSelect.selectedItem);
        console.log(treeSelect.selectedList);


